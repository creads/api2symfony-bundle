framework:
    secret: "supersecret"
    templating:
        engines: ['twig']
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
twig:
    debug: "%kernel.debug%"
    strict_variables: "%kernel.debug%"
